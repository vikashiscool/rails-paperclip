<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @recipe.title %>
</p>

<p>
  <strong>Instructions:</strong>
  <%= @recipe.instructions %>
</p>

<%= image_tag @recipe.preview.url(:small) %><br>
<%= image_tag @recipe.preview.url(:small) %><br>
<%= image_tag @recipe.preview.url(:medium) %><br>

<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>
                                                           